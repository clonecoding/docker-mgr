<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jdddata.dockermgr.bussiness.mapper.ProjectDeployInfoMapper">


    <insert id="insertProjectDeployInfo"  useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `project_deploy_info`
        (
        <trim suffixOverrides=",">
                    <if test="id!=null">
                        `id`,
                    </if>
                    <if test="projectId!=null">
                        `project_id`,
                    </if>
                    <if test="containerIp!=null">
                        `container_ip`,
                    </if>
                    <if test="env!=null">
                        `env`,
                    </if>
        </trim>
        )
        VALUES
        (
        <trim suffixOverrides=",">
                <if test="id!=null">
                    #{id},
                </if>
                <if test="projectId!=null">
                    #{projectId},
                </if>
                <if test="containerIp!=null">
                    #{containerIp},
                </if>
                <if test="env!=null">
                    #{env},
                </if>
        </trim>
        )
    </insert>




    <update id="updateProjectDeployInfo">
        UPDATE `project_deploy_info`
        SET
        <trim suffixOverrides=",">
            <if test="projectId != null and projectId!=''">
                `project_id` = #{projectId},
            </if>
            <if test="containerIp != null and containerIp!=''">
                `container_ip` = #{containerIp},
            </if>
            <if test="env != null and env!=''">
                `env` = #{env},
            </if>
        </trim>
        WHERE
        <trim suffixOverrides="and">
                `id` = #{id} and
        </trim>
    </update>

    <update id="update">
        UPDATE `project_deploy_info`
        SET
        <trim suffixOverrides=",">
        <if test="set.projectId != null and set.projectId!=''">
            `project_id` = #{set.projectId},
        </if>
        <if test="set.containerIp != null and set.containerIp!=''">
            `container_ip` = #{set.containerIp},
        </if>
        <if test="set.env != null and set.env!=''">
            `env` = #{set.env},
        </if>
        </trim>
        <trim prefix="where" suffixOverrides="and | or">
                <if test="where.idList != null">
                    `id` in
                    <foreach collection="where.idList" close=")" open="(" separator="," item="item">
                        #{item}
                    </foreach> and
                </if>
            <if test="where.idSt !=null">
                `id` >= #{where.idSt} and
            </if>
            <if test="where.idEd!=null">
                `id` &lt;= #{where.idEd} and
            </if>
                <if test="where.projectIdList != null">
                    `project_id` in
                    <foreach collection="where.projectIdList" close=")" open="(" separator="," item="item">
                        #{item}
                    </foreach> and
                </if>
            <if test ="where.fuzzyProjectId!=null and where.fuzzyProjectId.size()>0">
                (
                <foreach collection="where.fuzzyProjectId"  separator="or" item="item">
                    `Project_id` like concat('%',#{item},'%')
                </foreach>
                ) and
            </if>
            <if test ="where.rightFuzzyProjectId!=null and where.rightFuzzyProjectId.size()>0">
                (
                <foreach collection="where.rightFuzzyProjectId"  separator="or" item="item">
                    `Project_id` like concat(#{item},'%')
                </foreach>
                ) and
            </if>
                <if test="where.containerIpList != null">
                    `container_ip` in
                    <foreach collection="where.containerIpList" close=")" open="(" separator="," item="item">
                        #{item}
                    </foreach> and
                </if>
            <if test ="where.fuzzyContainerIp!=null and where.fuzzyContainerIp.size()>0">
                (
                <foreach collection="where.fuzzyContainerIp"  separator="or" item="item">
                    `Container_ip` like concat('%',#{item},'%')
                </foreach>
                ) and
            </if>
            <if test ="where.rightFuzzyContainerIp!=null and where.rightFuzzyContainerIp.size()>0">
                (
                <foreach collection="where.rightFuzzyContainerIp"  separator="or" item="item">
                    `Container_ip` like concat(#{item},'%')
                </foreach>
                ) and
            </if>
                <if test="where.envList != null">
                    `env` in
                    <foreach collection="where.envList" close=")" open="(" separator="," item="item">
                        #{item}
                    </foreach> and
                </if>
            <if test ="where.fuzzyEnv!=null and where.fuzzyEnv.size()>0">
                (
                <foreach collection="where.fuzzyEnv"  separator="or" item="item">
                    `Env` like concat('%',#{item},'%')
                </foreach>
                ) and
            </if>
            <if test ="where.rightFuzzyEnv!=null and where.rightFuzzyEnv.size()>0">
                (
                <foreach collection="where.rightFuzzyEnv"  separator="or" item="item">
                    `Env` like concat(#{item},'%')
                </foreach>
                ) and
            </if>
        </trim>
    </update>


    <resultMap type="com.jdddata.dockermgr.bussiness.modle.ProjectDeployInfo" id="ProjectDeployInfoMap">
        <result property="id" column="id"/>
        <result property="projectId" column="project_id"/>
        <result property="containerIp" column="container_ip"/>
        <result property="env" column="env"/>
    </resultMap>


    <select id="queryProjectDeployInfo" resultMap="ProjectDeployInfoMap">
        select
        <include refid="baseResult"></include>
        from  `project_deploy_info`
        <trim prefix="where" suffixOverrides="and | or">
            <if test="id != null">
                `id` = #{id} and
            </if>
            <if test="projectId != null and projectId!=''">
                `project_id` = #{projectId} and
            </if>
            <if test="containerIp != null and containerIp!=''">
                `container_ip` = #{containerIp} and
            </if>
            <if test="env != null and env!=''">
                `env` = #{env} and
            </if>
            <if test = "(_parameter instanceof .ProjectDeployInfo$QueryBuilder) == true">
                <if test="idList != null">
                    `id` in
                    <foreach collection="idList" close=")" open="(" separator="," item="item">
                        #{item}
                    </foreach> and
                </if>
                <if test="idSt !=null">
                    `id` >= #{idSt} and
                </if>
                <if test="idEd!=null">
                    `id` &lt;= #{idEd} and
                </if>
                <if test="projectIdList != null">
                    `project_id` in
                    <foreach collection="projectIdList" close=")" open="(" separator="," item="item">
                        #{item}
                    </foreach> and
                </if>
                <if test ="fuzzyProjectId!=null and fuzzyProjectId.size()>0">
                    (
                    <foreach collection="fuzzyProjectId"  separator="or" item="item">
                        `Project_id` like concat('%',#{item},'%')
                    </foreach>
                    ) and
                </if>
                <if test ="rightFuzzyProjectId!=null and rightFuzzyProjectId.size()>0">
                    (
                    <foreach collection="rightFuzzyProjectId"  separator="or" item="item">
                        `Project_id` like concat(#{item},'%')
                    </foreach>
                    ) and
                </if>
                <if test="containerIpList != null">
                    `container_ip` in
                    <foreach collection="containerIpList" close=")" open="(" separator="," item="item">
                        #{item}
                    </foreach> and
                </if>
                <if test ="fuzzyContainerIp!=null and fuzzyContainerIp.size()>0">
                    (
                    <foreach collection="fuzzyContainerIp"  separator="or" item="item">
                        `Container_ip` like concat('%',#{item},'%')
                    </foreach>
                    ) and
                </if>
                <if test ="rightFuzzyContainerIp!=null and rightFuzzyContainerIp.size()>0">
                    (
                    <foreach collection="rightFuzzyContainerIp"  separator="or" item="item">
                        `Container_ip` like concat(#{item},'%')
                    </foreach>
                    ) and
                </if>
                <if test="envList != null">
                    `env` in
                    <foreach collection="envList" close=")" open="(" separator="," item="item">
                        #{item}
                    </foreach> and
                </if>
                <if test ="fuzzyEnv!=null and fuzzyEnv.size()>0">
                    (
                    <foreach collection="fuzzyEnv"  separator="or" item="item">
                        `Env` like concat('%',#{item},'%')
                    </foreach>
                    ) and
                </if>
                <if test ="rightFuzzyEnv!=null and rightFuzzyEnv.size()>0">
                    (
                    <foreach collection="rightFuzzyEnv"  separator="or" item="item">
                        `Env` like concat(#{item},'%')
                    </foreach>
                    ) and
                </if>
            </if>
        </trim>
    </select>

    <select id="queryProjectDeployInfoLimit1" resultMap="ProjectDeployInfoMap">
        select
        <include refid="baseResult"></include>
        from  `project_deploy_info`
        <trim prefix="where" suffixOverrides="and | or">
            <if test="id != null">
                `id` = #{id} and
            </if>
            <if test="projectId != null and projectId!=''">
                `project_id` = #{projectId} and
            </if>
            <if test="containerIp != null and containerIp!=''">
                `container_ip` = #{containerIp} and
            </if>
            <if test="env != null and env!=''">
                `env` = #{env} and
            </if>
            <if test = "(_parameter instanceof com.jdddata.dockermgr.bussiness.modle.ProjectDeployInfo$QueryBuilder) == true">
                    <if test="idList != null">
                        `id` in
                        <foreach collection="idList" close=")" open="(" separator="," item="item">
                            #{item}
                        </foreach> and
                    </if>
                <if test="idSt !=null">
                    `id` >= #{idSt} and
                </if>
                <if test="idEd!=null">
                    `id` &lt;= #{idEd} and
                </if>
                    <if test="projectIdList != null">
                        `project_id` in
                        <foreach collection="projectIdList" close=")" open="(" separator="," item="item">
                            #{item}
                        </foreach> and
                    </if>
                <if test ="fuzzyProjectId!=null and fuzzyProjectId.size()>0">
                    (
                    <foreach collection="fuzzyProjectId"  separator="or" item="item">
                        `Project_id` like concat('%',#{item},'%')
                    </foreach>
                    ) and
                </if>
                <if test ="rightFuzzyProjectId!=null and rightFuzzyProjectId.size()>0">
                    (
                    <foreach collection="rightFuzzyProjectId"  separator="or" item="item">
                        `Project_id` like concat(#{item},'%')
                    </foreach>
                    ) and
                </if>
                    <if test="containerIpList != null">
                        `container_ip` in
                        <foreach collection="containerIpList" close=")" open="(" separator="," item="item">
                            #{item}
                        </foreach> and
                    </if>
                <if test ="fuzzyContainerIp!=null and fuzzyContainerIp.size()>0">
                    (
                    <foreach collection="fuzzyContainerIp"  separator="or" item="item">
                        `Container_ip` like concat('%',#{item},'%')
                    </foreach>
                    ) and
                </if>
                <if test ="rightFuzzyContainerIp!=null and rightFuzzyContainerIp.size()>0">
                    (
                    <foreach collection="rightFuzzyContainerIp"  separator="or" item="item">
                        `Container_ip` like concat(#{item},'%')
                    </foreach>
                    ) and
                </if>
                    <if test="envList != null">
                        `env` in
                        <foreach collection="envList" close=")" open="(" separator="," item="item">
                            #{item}
                        </foreach> and
                    </if>
                <if test ="fuzzyEnv!=null and fuzzyEnv.size()>0">
                    (
                    <foreach collection="fuzzyEnv"  separator="or" item="item">
                        `Env` like concat('%',#{item},'%')
                    </foreach>
                    ) and
                </if>
                <if test ="rightFuzzyEnv!=null and rightFuzzyEnv.size()>0">
                    (
                    <foreach collection="rightFuzzyEnv"  separator="or" item="item">
                        `Env` like concat(#{item},'%')
                    </foreach>
                    ) and
                </if>
            </if>
        </trim>
        limit 1
    </select>

    <sql id="allResult">
        <trim suffixOverrides=",">
        `id`,
        `project_id`,
        `container_ip`,
        `env`,
        </trim>
    </sql>



    <sql id="baseResult">
        <trim suffixOverrides=",">
            <if test = "(_parameter instanceof com.jdddata.dockermgr.bussiness.modle.ProjectDeployInfo$QueryBuilder) == true">

                <if test="fetchFields==null">
                    <include refid="allResult"></include>
                </if>
                <if test="fetchFields!=null">
                    <if test="fetchFields.AllFields !=null">
                        <include refid="allResult"></include>
                    </if>
                    <if test="fetchFields.AllFields ==null and fetchFields.fetchFields==null and fetchFields.excludeFields==null and fetchFields.otherFields==null">
                        <include refid="allResult"></include>
                    </if>
                    <if test="fetchFields.AllFields==null and fetchFields.fetchFields!=null">
                    <if test="fetchFields.fetchFields.id==true">
                        `id`,
                    </if>
                    <if test="fetchFields.fetchFields.projectId==true">
                        `project_id`,
                    </if>
                    <if test="fetchFields.fetchFields.containerIp==true">
                        `container_ip`,
                    </if>
                    <if test="fetchFields.fetchFields.env==true">
                        `env`,
                    </if>
                    </if>
                    <if test="fetchFields.AllFields==null and fetchFields.excludeFields!=null">
                    <if test="fetchFields.excludeFields.id==null">
                        `id`,
                    </if>
                    <if test="fetchFields.excludeFields.projectId==null">
                        `project_id`,
                    </if>
                    <if test="fetchFields.excludeFields.containerIp==null">
                        `container_ip`,
                    </if>
                    <if test="fetchFields.excludeFields.env==null">
                        `env`,
                    </if>
                    </if>
                    <if test="fetchFields.otherFields!=null and fetchFields.otherFields.size>0">
                        <foreach collection="fetchFields.otherFields" index="index" item="item" separator=",">
                        `#{item}`
                        </foreach>
                    </if>
                </if>
            </if>
            <if test="(_parameter instanceof .ProjectDeployInfo$QueryBuilder) == false" >
                <include refid="allResult"></include>
            </if>

        </trim>
    </sql>


</mapper>