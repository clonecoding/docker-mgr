<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jdddata.dockermgr.dao.mapper.ImagesMgrMapper">


    <insert id="insertImagesMgr" >
        INSERT INTO `images_mgr`
        (
        <trim suffixOverrides=",">
                    <if test="id!=null">
                        `id`,
                    </if>
                    <if test="imageName!=null">
                        `imageName`,
                    </if>
                    <if test="imageTag!=null">
                        `imageTag`,
                    </if>
                    <if test="createTime!=null">
                        `createTime`,
                    </if>
        </trim>
        )
        VALUES
        (
        <trim suffixOverrides=",">
                <if test="id!=null">
                    #{id},
                </if>
                <if test="imageName!=null">
                    #{imageName},
                </if>
                <if test="imageTag!=null">
                    #{imageTag},
                </if>
                <if test="createTime!=null">
                    #{createTime},
                </if>
        </trim>
        )
    </insert>




    <update id="updateImagesMgr">
        UPDATE `images_mgr`
        SET
        <trim suffixOverrides=",">
            <if test="id != null and id!=''">
                `id` = #{id},
            </if>
            <if test="imageName != null and imageName!=''">
                `imageName` = #{imageName},
            </if>
            <if test="imageTag != null and imageTag!=''">
                `imageTag` = #{imageTag},
            </if>
            <if test="createTime != null">
                `createTime` = #{createTime},
            </if>
        </trim>
        WHERE
        <trim suffixOverrides="and">
        </trim>
    </update>

    <update id="update">
        UPDATE `images_mgr`
        SET
        <trim suffixOverrides=",">
        <if test="set.id != null and set.id!=''">
            `id` = #{set.id},
        </if>
        <if test="set.imageName != null and set.imageName!=''">
            `imageName` = #{set.imageName},
        </if>
        <if test="set.imageTag != null and set.imageTag!=''">
            `imageTag` = #{set.imageTag},
        </if>
        <if test="set.createTime != null">
            `createTime` = #{set.createTime},
        </if>
        </trim>
        <trim prefix="where" suffixOverrides="and | or">
                <if test="where.idList != null">
                    `id` in
                    <foreach collection="where.idList" close=")" open="(" separator="," item="item">
                        #{item}
                    </foreach> and
                </if>
            <if test ="where.fuzzyId!=null and where.fuzzyId.size()>0">
                (
                <foreach collection="where.fuzzyId"  separator="or" item="item">
                    `Id` like concat('%',#{item},'%')
                </foreach>
                ) and
            </if>
            <if test ="where.rightFuzzyId!=null and where.rightFuzzyId.size()>0">
                (
                <foreach collection="where.rightFuzzyId"  separator="or" item="item">
                    `Id` like concat(#{item},'%')
                </foreach>
                ) and
            </if>
                <if test="where.imageNameList != null">
                    `imageName` in
                    <foreach collection="where.imageNameList" close=")" open="(" separator="," item="item">
                        #{item}
                    </foreach> and
                </if>
            <if test ="where.fuzzyImageName!=null and where.fuzzyImageName.size()>0">
                (
                <foreach collection="where.fuzzyImageName"  separator="or" item="item">
                    `ImageName` like concat('%',#{item},'%')
                </foreach>
                ) and
            </if>
            <if test ="where.rightFuzzyImageName!=null and where.rightFuzzyImageName.size()>0">
                (
                <foreach collection="where.rightFuzzyImageName"  separator="or" item="item">
                    `ImageName` like concat(#{item},'%')
                </foreach>
                ) and
            </if>
                <if test="where.imageTagList != null">
                    `imageTag` in
                    <foreach collection="where.imageTagList" close=")" open="(" separator="," item="item">
                        #{item}
                    </foreach> and
                </if>
            <if test ="where.fuzzyImageTag!=null and where.fuzzyImageTag.size()>0">
                (
                <foreach collection="where.fuzzyImageTag"  separator="or" item="item">
                    `ImageTag` like concat('%',#{item},'%')
                </foreach>
                ) and
            </if>
            <if test ="where.rightFuzzyImageTag!=null and where.rightFuzzyImageTag.size()>0">
                (
                <foreach collection="where.rightFuzzyImageTag"  separator="or" item="item">
                    `ImageTag` like concat(#{item},'%')
                </foreach>
                ) and
            </if>
                <if test="where.createTimeList != null">
                    `createTime` in
                    <foreach collection="where.createTimeList" close=")" open="(" separator="," item="item">
                        #{item}
                    </foreach> and
                </if>
            <if test="where.createTimeSt !=null">
                `createTime` >= #{where.createTimeSt} and
            </if>
            <if test="where.createTimeEd!=null">
                `createTime` &lt;= #{where.createTimeEd} and
            </if>
        </trim>
    </update>


    <resultMap type="com.jdddata.dockermgr.dao.modle.ImagesMgr" id="ImagesMgrMap">
        <result property="id" column="id"/>
        <result property="imageName" column="imageName"/>
        <result property="imageTag" column="imageTag"/>
        <result property="createTime" column="createTime"/>
    </resultMap>


    <select id="queryImagesMgr" resultMap="ImagesMgrMap">
        select
        <include refid="baseResult"></include>
        from  `images_mgr`
        <trim prefix="where" suffixOverrides="and | or">
            <if test="id != null and id!=''">
                `id` = #{id} and
            </if>
            <if test="imageName != null and imageName!=''">
                `imageName` = #{imageName} and
            </if>
            <if test="imageTag != null and imageTag!=''">
                `imageTag` = #{imageTag} and
            </if>
            <if test="createTime != null">
                `createTime` = #{createTime} and
            </if>
            <if test = "(_parameter instanceof com.jdddata.dockermgr.dao.modle.ImagesMgr$QueryBuilder) == true">
                <if test="idList != null">
                    `id` in
                    <foreach collection="idList" close=")" open="(" separator="," item="item">
                        #{item}
                    </foreach> and
                </if>
                <if test ="fuzzyId!=null and fuzzyId.size()>0">
                    (
                    <foreach collection="fuzzyId"  separator="or" item="item">
                        `Id` like concat('%',#{item},'%')
                    </foreach>
                    ) and
                </if>
                <if test ="rightFuzzyId!=null and rightFuzzyId.size()>0">
                    (
                    <foreach collection="rightFuzzyId"  separator="or" item="item">
                        `Id` like concat(#{item},'%')
                    </foreach>
                    ) and
                </if>
                <if test="imageNameList != null">
                    `imageName` in
                    <foreach collection="imageNameList" close=")" open="(" separator="," item="item">
                        #{item}
                    </foreach> and
                </if>
                <if test ="fuzzyImageName!=null and fuzzyImageName.size()>0">
                    (
                    <foreach collection="fuzzyImageName"  separator="or" item="item">
                        `ImageName` like concat('%',#{item},'%')
                    </foreach>
                    ) and
                </if>
                <if test ="rightFuzzyImageName!=null and rightFuzzyImageName.size()>0">
                    (
                    <foreach collection="rightFuzzyImageName"  separator="or" item="item">
                        `ImageName` like concat(#{item},'%')
                    </foreach>
                    ) and
                </if>
                <if test="imageTagList != null">
                    `imageTag` in
                    <foreach collection="imageTagList" close=")" open="(" separator="," item="item">
                        #{item}
                    </foreach> and
                </if>
                <if test ="fuzzyImageTag!=null and fuzzyImageTag.size()>0">
                    (
                    <foreach collection="fuzzyImageTag"  separator="or" item="item">
                        `ImageTag` like concat('%',#{item},'%')
                    </foreach>
                    ) and
                </if>
                <if test ="rightFuzzyImageTag!=null and rightFuzzyImageTag.size()>0">
                    (
                    <foreach collection="rightFuzzyImageTag"  separator="or" item="item">
                        `ImageTag` like concat(#{item},'%')
                    </foreach>
                    ) and
                </if>
                <if test="createTimeList != null">
                    `createTime` in
                    <foreach collection="createTimeList" close=")" open="(" separator="," item="item">
                        #{item}
                    </foreach> and
                </if>
                <if test="createTimeSt !=null">
                    `createTime` >= #{createTimeSt} and
                </if>
                <if test="createTimeEd!=null">
                    `createTime` &lt;= #{createTimeEd} and
                </if>
            </if>
        </trim>
    </select>

    <select id="queryImagesMgrLimit1" resultMap="ImagesMgrMap">
        select
        <include refid="baseResult"></include>
        from  `images_mgr`
        <trim prefix="where" suffixOverrides="and | or">
            <if test="id != null and id!=''">
                `id` = #{id} and
            </if>
            <if test="imageName != null and imageName!=''">
                `imageName` = #{imageName} and
            </if>
            <if test="imageTag != null and imageTag!=''">
                `imageTag` = #{imageTag} and
            </if>
            <if test="createTime != null">
                `createTime` = #{createTime} and
            </if>
            <if test = "(_parameter instanceof com.jdddata.dockermgr.dao.modle.ImagesMgr$QueryBuilder) == true">
                    <if test="idList != null">
                        `id` in
                        <foreach collection="idList" close=")" open="(" separator="," item="item">
                            #{item}
                        </foreach> and
                    </if>
                <if test ="fuzzyId!=null and fuzzyId.size()>0">
                    (
                    <foreach collection="fuzzyId"  separator="or" item="item">
                        `Id` like concat('%',#{item},'%')
                    </foreach>
                    ) and
                </if>
                <if test ="rightFuzzyId!=null and rightFuzzyId.size()>0">
                    (
                    <foreach collection="rightFuzzyId"  separator="or" item="item">
                        `Id` like concat(#{item},'%')
                    </foreach>
                    ) and
                </if>
                    <if test="imageNameList != null">
                        `imageName` in
                        <foreach collection="imageNameList" close=")" open="(" separator="," item="item">
                            #{item}
                        </foreach> and
                    </if>
                <if test ="fuzzyImageName!=null and fuzzyImageName.size()>0">
                    (
                    <foreach collection="fuzzyImageName"  separator="or" item="item">
                        `ImageName` like concat('%',#{item},'%')
                    </foreach>
                    ) and
                </if>
                <if test ="rightFuzzyImageName!=null and rightFuzzyImageName.size()>0">
                    (
                    <foreach collection="rightFuzzyImageName"  separator="or" item="item">
                        `ImageName` like concat(#{item},'%')
                    </foreach>
                    ) and
                </if>
                    <if test="imageTagList != null">
                        `imageTag` in
                        <foreach collection="imageTagList" close=")" open="(" separator="," item="item">
                            #{item}
                        </foreach> and
                    </if>
                <if test ="fuzzyImageTag!=null and fuzzyImageTag.size()>0">
                    (
                    <foreach collection="fuzzyImageTag"  separator="or" item="item">
                        `ImageTag` like concat('%',#{item},'%')
                    </foreach>
                    ) and
                </if>
                <if test ="rightFuzzyImageTag!=null and rightFuzzyImageTag.size()>0">
                    (
                    <foreach collection="rightFuzzyImageTag"  separator="or" item="item">
                        `ImageTag` like concat(#{item},'%')
                    </foreach>
                    ) and
                </if>
                    <if test="createTimeList != null">
                        `createTime` in
                        <foreach collection="createTimeList" close=")" open="(" separator="," item="item">
                            #{item}
                        </foreach> and
                    </if>
                <if test="createTimeSt !=null">
                    `createTime` >= #{createTimeSt} and
                </if>
                <if test="createTimeEd!=null">
                    `createTime` &lt;= #{createTimeEd} and
                </if>
            </if>
        </trim>
        limit 1
    </select>

    <sql id="allResult">
        <trim suffixOverrides=",">
        `id`,
        `imageName`,
        `imageTag`,
        `createTime`,
        </trim>
    </sql>



    <sql id="baseResult">
        <trim suffixOverrides=",">
            <if test = "(_parameter instanceof .com.jdddata.dockermgr.dao.modle.ImagesMgr$QueryBuilder) == true">

                <if test="fetchFields==null">
                    <include refid="allResult"></include>
                </if>
                <if test="fetchFields!=null">
                    <if test="fetchFields.AllFields !=null">
                        <include refid="allResult"></include>
                    </if>
                    <if test="fetchFields.AllFields ==null and fetchFields.fetchFields==null and fetchFields.excludeFields==null and fetchFields.otherFields==null">
                        <include refid="allResult"></include>
                    </if>
                    <if test="fetchFields.AllFields==null and fetchFields.fetchFields!=null">
                    <if test="fetchFields.fetchFields.id==true">
                        `id`,
                    </if>
                    <if test="fetchFields.fetchFields.imageName==true">
                        `imageName`,
                    </if>
                    <if test="fetchFields.fetchFields.imageTag==true">
                        `imageTag`,
                    </if>
                    <if test="fetchFields.fetchFields.createTime==true">
                        `createTime`,
                    </if>
                    </if>
                    <if test="fetchFields.AllFields==null and fetchFields.excludeFields!=null">
                    <if test="fetchFields.excludeFields.id==null">
                        `id`,
                    </if>
                    <if test="fetchFields.excludeFields.imageName==null">
                        `imageName`,
                    </if>
                    <if test="fetchFields.excludeFields.imageTag==null">
                        `imageTag`,
                    </if>
                    <if test="fetchFields.excludeFields.createTime==null">
                        `createTime`,
                    </if>
                    </if>
                    <if test="fetchFields.otherFields!=null and fetchFields.otherFields.size>0">
                        <foreach collection="fetchFields.otherFields" index="index" item="item" separator=",">
                        `#{item}`
                        </foreach>
                    </if>
                </if>
            </if>
            <if test="(_parameter instanceof .com.jdddata.dockermgr.dao.modle.ImagesMgr$QueryBuilder) == false" >
                <include refid="allResult"></include>
            </if>

        </trim>
    </sql>


</mapper>